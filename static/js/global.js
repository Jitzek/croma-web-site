const USERNAME = 'Jitzek';
const PROJECT = 'croma-web-site';
const BRANCH = 'master';

function LFSpath(path) {
    return `https://media.githubusercontent.com/media/${USERNAME}/${PROJECT}/${BRANCH}/${path}`;
}